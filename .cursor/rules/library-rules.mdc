---
description: 使用第三方 npm 套件時，必須以本專案安裝版本與 node_modules 為準，禁止未驗證的 API／預設行為範例
alwaysApply: false
---

# 第三方套件（npm）使用與範例規範

## 適用時機

只要回覆涉及：**import 某套件、呼叫其 API、預設行為、型別、可貼程式碼範例、升級影響**，一律遵守本規則。

## 強制流程（回答前）

1. **確認版本**：讀本專案根目錄 `package.json`（必要時含 lockfile）中該套件的**實際版本**。
2. **以本專案 `node_modules/<套件名>/` 為唯一權威**：
   - 先讀 `package.json` 的 `main` / `types` / `exports`，找到入口與型別檔。
   - 讀對應 `*.d.ts` 取得**函式簽名與參數順序**。
   - 若問題涉及**預設參數、執行時才決定的回傳欄位**（例如 `recognize` 預設 `output`），而 d.ts 未寫清楚，必須讀該套件**實際發佈的 JS 原始碼**（常見於 `src/*.js` 或 `dist`）。
3. **禁止**僅依記憶、舊版文件、或通用教學文章當成「已定論」；若當下無法讀 `node_modules`，必須明確寫出「**未在本 repo 驗證**」，並列出使用者可自行核對的檔案路徑與步驟。

## 提供範例時

- 範例必須與**步驟 1 查到的版本**一致。
- 必附**依據**：至少寫出「套件名@版本 + 依據檔案相對路徑」（例如 `node_modules/tesseract.js/src/createWorker.js`）與關鍵行為一句話（例如預設第三參數 `output` 內容）。
- 若 API 有多種呼叫型式，優先給**與本專案現有 import／bundler（如 Vite）相容**的寫法。

## 與本專案其他規則的關係

- 不因此改變 `docs/INFO.md` 定義的架構邊界；僅確保**套件用法與本機安裝版本一致**。
